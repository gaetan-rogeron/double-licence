exerice 1 :

2. Ca compile pas il y a des erreures exit 1
3. c n'etait pas declarer, n melanger declaration et calcul, et main ne retourner pas return 0 avec int en signature

exerice 3

pour la q 2 et 3 
int main(void){
    int valeur;
    for (;;) {
        valeur = lire_nombre();
        if (valeur == -1){
            printf("Ce n'est pas un chiffre.\n");
        }
        else {
            if (valeur %2 == 0){
                printf("L'entier %d est pair \n", valeur);
            }
            else {
                printf("L'entier %d est impair \n", valeur);
            }
        }
    }
    return 0;
}

pour la 4 
int main(void){
    int valeur;
    valeur = moyenne_stdin();
    printf("La moyenne est %d", valeur);
    return 0;
}

exercice 6
0 car ca depasse la valeur des int
c'est a partir de 49 que ca renvoie une mauvaise valeur
